services:
  otel-collector:
    environment:
      LANGSMITH_API_KEY: ${LANGSMITH_API_KEY:?set LANGSMITH_API_KEY}
      LANGSMITH_PROJECT: ${LANGSMITH_PROJECT:-racklens-reliability-prod}
    volumes:
      - ./otel-collector-langsmith.yaml:/etc/otelcol-contrib/config.yaml:ro
